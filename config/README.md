# config

Repo-safe example configuration belongs here.

Use this folder for sample universe, timeframe, and provider settings that explain how research runs are shaped without exposing secrets.

Current examples:

- `markets.example.yaml` for the public Diamond Hands market-analysis run
- `diamond-hands.example.yaml` for the public-to-private bridge shape that forks can adapt locally
